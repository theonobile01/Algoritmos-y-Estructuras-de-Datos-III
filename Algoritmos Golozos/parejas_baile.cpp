#include <vector>
#include <iostream>
#include <cmath>
using namespace std;


int main(){
    int a;
    cin >> a;
    vector<int>conjuntoA(a);
    vector<int>conjuntoB(a);

    for (int i= 0; i < a; i++){
        cin >> conjuntoA[i];
    }
    for (int j = 0; j < a; j++){
        cin >> conjuntoB[j];
    }
    for 
    return res;
}